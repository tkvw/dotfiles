
# Personal preferences

## Language
- Communicate with me in Dutch
- All repo output in English: code, comments, docs, commits, CLI examples

## Responses
- Concise and to the point, skip unnecessary explanation unless asked
- CLI snippets in nushell syntax

## Environment
{{ if .is_win -}}
- Windows 11, using git bash
- Use proper escaping for git bash when running commands
{{ else if .is_wsl -}}
- Ubuntu 24 in WSL
{{ else if .is_docker -}}
- Linux in docker
{{ end -}}
- Editor: nvim (LazyVim)
- Shell: nushell
- Prefer writing scripts to file over inline -c/-C flags to avoid escaping issues

## VCS
- Conventional commits, imperative mood, max 72 chars
- Small focused commits, one logical change each
- Ask confirmation before committing
